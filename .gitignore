# Build artifacts
bin/
dist/
out/
*.exe
*.test
*.out
# Per-service compiled Go binaries (path matches service name).
services/ct-monitor/ct-monitor
services/verdict-api/verdict-api
services/resolver/resolver
services/portal-api/portal-api
services/scheduler/scheduler
services/healthcheck/healthcheck
services/registry-svc/registry-svc

# Go
vendor/
# Exception: extension vendors JS libraries (e.g. jsQR) that ARE source-
# of-truth and must ship in the repo.
!apps/extension/src/vendor/
!apps/extension/src/vendor/**

# Node / Next.js
node_modules/
.next/
.turbo/
*.tsbuildinfo
.env*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Data / generated
data/
evidence/
*.dump
*.sqlite

# Brand registry: manual screenshots are visual IP, keep them out of git.
# Keep the directory itself via .gitkeep.
tools/brand-seeder/manual/*
!tools/brand-seeder/manual/.gitkeep

# Local secrets
*.pem
*.key
!tls/cert.pem.example
.env

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# CI / coverage
coverage.txt
coverage.html

# Chrome extension auto-generated indexed rulesets (regenerated on load)
apps/extension/_metadata/

# Debug HAR captures + ad-hoc inspection files (3 MB+ each, not source)
*.har
/uflix
