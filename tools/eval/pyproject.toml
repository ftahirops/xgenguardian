[project]
name = "xgg-eval"
version = "0.1.0"
description = "XGenGuardian — Phase-1 evaluation harness against PhishTank + Tranco"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27",
  "rich>=13.7",
]
