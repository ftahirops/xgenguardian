[project]
name = "xgg-brand-seeder"
version = "0.1.0"
description = "XGenGuardian — seeds the Brand Registry from brands.yaml"
requires-python = ">=3.11"
dependencies = [
  "pyyaml>=6.0",
  "httpx>=0.27",
  "psycopg[binary]>=3.2",
  "pgvector>=0.3",
  "Pillow>=10.3",
  "imagehash>=4.3",
]
