[project]
name = "xgg-blocklist-fetcher"
version = "0.1.0"
description = "XGenGuardian — hourly ingest of phishing/malware blocklists"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27",
  "redis>=5.0",
]
